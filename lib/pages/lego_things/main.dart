import 'package:flutter/material.dart';

class LegoThingsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: Center(
        child: Text("All Lego Things is here."),
      ),
    );
  }
}
